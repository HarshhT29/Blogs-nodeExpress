<!DOCTYPE html>
<html lang="en">
<head>
    <link href="/css/tailwind.css" rel="stylesheet">
    <title>Register</title>
</head>
<body class="bg-gray-100">
    <%- include("./partials/navbar.ejs") %>
    <div class="container mx-auto flex flex-col items-center justify-center ">
        <h1 class="my-5 font-bold text-3xl">Create an account</h1>
        <form action="/user/register" method="post" class="flex flex-col items-center justify-center w-full max-w-sm bg-white border-2 rounded-lg border-gray-300 shadow-lg p-8 space-y-4">
            <label for="name" class="block w-full">
                <span class="block text-gray-700 font-medium mb-2">Name</span>
                <input type="text" name="name" required class="w-full border-2 rounded-lg p-2" />
            </label>
            <label for="email" class="block w-full">
                <span class="block text-gray-700 font-medium mb-2">Email</span>
                <input type="email" name="email" required class="w-full border-2 rounded-lg p-2" />
            </label>
            <label for="password" class="block w-full">
                <span class="block text-gray-700 font-medium mb-2">Password</span>
                <input type="text" name="password" required class="w-full border-2 rounded-lg p-2" />
            </label>
            <button type="submit" class="mt-4 px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600">Register</button>
        </form>
        <% if(error) { %>
            <p class="text-red-500 mt-2"><%= error %></p>
        <% } %>
    </div>
</body>
</html>
